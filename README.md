⚡ Quick Start

This README provides the essential commands to install and run your Next.js application locally.


1. Requirements
2. Make sure you have Node.js and npm (or Yarn) installed.
3. 2. Setup & Run
  
      Open your command prompt or terminal, navigate to the project's root folder, and execute the following two commands:

      Install:
      npm install
      Downloads and installs all project dependencies.

      Start:
      npm run dev
      Starts the development server with hot reloading.
   
      Your application will typically run at: http://localhost:3000
      
